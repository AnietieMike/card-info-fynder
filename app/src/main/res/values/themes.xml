<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CardInfoFynder" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/island_green</item>
        <item name="colorPrimaryVariant">@color/island_green</item>
        <item name="colorOnPrimary">@color/sailor_blue</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/mint_500</item>
        <item name="colorSecondaryVariant">@color/mint_500</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.CardInfoFynder.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.CardInfoFynder.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        // Set the splash screen background, animated icon, and animation duration.
        <item name="windowSplashScreenBackground">@color/sailor_blue</item>

        // Use windowSplashScreenAnimatedIcon to add either a drawable or an
        // animated drawable. One of these is required.
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_baseline_credit_card_48</item>

        // Set the theme of the Activity that directly follows your splash screen.
        <item name="postSplashScreenTheme">@style/Theme.CardInfoFynder</item>
    </style>

    <style name="SplashTheme" parent="Theme.CardInfoFynder">
        <item name="android:windowBackground">@drawable/splash_screen_layer_list</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/sailor_blue</item>
    </style>

</resources>